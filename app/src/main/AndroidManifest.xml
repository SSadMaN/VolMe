<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sadman.volme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddEventForm" />
        <activity android:name=".Activity_Profile" />
        <activity
            android:name=".Activity_Bonuses"
            android:label="@string/title_activity_bonuses"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activity_Event"
            android:label="@string/title_activity_event"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".List_Subscribers"/>
    </application>

</manifest>